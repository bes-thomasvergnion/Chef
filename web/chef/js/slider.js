$(document).ready(function(){
  	$(".owl-carousel").owlCarousel({
  		loop:true,
	    margin:0,
	    dots:true,
	    smartSpeed: 1000,
	    autoplay:true,
	    autoplayTimeout:8000,
	    items: 1
  	});
});