{{ form_start(form, {'attr': {'class': 'search-form'}}) }}

    {# Les erreurs générales du formulaire. #}
    {{ form_errors(form) }}
    
    {{ form_widget(form.value, {'attr': {'class': 'search'}}) }}
    
    {{ form_widget(form.save, { 'label': '<i class="fa fa-search" aria-hidden="true"></i>' }) }}
    
{{ form_end(form) }}